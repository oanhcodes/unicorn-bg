<!DOCTYPE html>
<html>
<head>
  <title>Unicorn Game Database</title>
  <link rel="stylesheet" type="text/css" href="layout.css">
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <nav>
    <ul class="right">
      <% if current_user %>
        <li>Signed in as <%= current_user.name %> | <%= link_to "Logout", "/logout" %></li>
      <% else %>
        <li><%= link_to "Login", "/login" %> | <%= link_to "Sign-up", "/signup" %></li>
      <% end %>
    </ul>
  </nav>

  
  <header><a href="/">Title Goes Here!</a></header>

  <div class="main-container">
    <p class="flash"><%= flash.notice %></p>
    <%= yield %>
  </div>

  <footer>Brought to you by Team Unicorn</footer>
</body>

</html>